language: python
python:
    - 3.7.0
build:
    ci:
        - sudo apt-get install rabbitmq-server
        - make process_mng
        - make install_tester
        - pip install -r requirements.txt
        - make start
        - make test
